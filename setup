from setuptools import setup, find_packages

setup(
    name="oss_png_transfer",  # 라이브러리 이름
    version="0.1.0",            # 버전
    author="sijbwer77, gitleejm, yong8947, Nhh05, lovedosoon",
    author_email="leejm8422@hanyang.ac.kr", # 이 파일 제작자
    description="A library for PNG MINIST data handling, preprocessing, and simple MLP training",
    long_description=open("README.md", "r").read(),
    long_description_content_type="text/markdown",
    url="https://github.com/Nhh05/oss_data_library",  # GitHub 링크
    packages=find_packages(),
    classifiers=[
        "Programming Language :: Python :: 3",
        "License :: OSI Approved :: MIT License",
        "Operating System :: OS Independent",
    ],
    python_requires='>=3.6',
    install_requires=[
        "numpy",  # 의존성
    ],
)